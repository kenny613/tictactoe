import React, { useState } from "react";

const AI_title = (props) => {
  // 宣告一個新的 state 變數，我們稱作為「count」。
  const [count, setCount] = useState(0);

  return <div>123</div>;
};

export default AI_title;
